spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:config/
            classpath:config/prescriptionservice,
            classpath:config/medicineservice,
            classpath:config/cardservice
server:
  port: 8888
